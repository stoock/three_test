# Hot Wheels Downhill Racing (Diecast Gravity Race) — 조사 및 구현 계획

## 1. 조사 요약

"Hot Wheels Downhill Racing / Diecast Tournament Racing / Gravity Drag Race"는
1:64 다이캐스트 미니카를 **모터 없이 중력만으로** 경사 트랙에서 굴려 승부를 가리는
실제 존재하는 하비/스포츠 장르다. 대표적으로 3DBotMaker(King of the Mountain 등)가
디오라마 산악 트랙에서 중계 형식의 영상을 만들며, Redline Derby 등 리그도 있다.

### 실제 경기의 핵심 요소 (조사 결과)
| 요소 | 실제 경기에서의 의미 |
|---|---|
| **트랙** | 약 3/4 다운힐 + 마지막 1/4 평지 런아웃. 측벽이 높아 차가 이탈하지 않고 벽을 긁으며 감속 |
| **무게** | 무거울수록 공기저항·베어링 저항 대비 유리 → 평지 런아웃에서 강함. 단, 액슬(지름 1mm 강선)이 휘면 휠이 쓸려 역효과 — 최적점이 존재한다 |
| **휠/액슬** | FTE(Faster Than Ever) 휠, 액슬 폴리싱+윤활이 핵심 튜닝. 구름저항/베어링 마찰 감소 |
| **무게 배분** | 낮고 중앙에 가까운 무게중심 = 코너에서 벽 긁힘(스크럽) 손실 감소, 안정성 |
| **레인 선택** | 코너 안/바깥 레인의 곡률·길이 차이가 기록에 영향 |
| **점프** | 크레스트(정점)에서 v²·κ > g·cosθ 이면 차가 떠서 비행 — "Big Air Flying Finish" 보너스 문화 |

출처:
- [MEL Magazine — Inside the World of Hot Wheels Rally Car Racing](https://melmagazine.com/en-us/story/hot-wheels-rally-car-racing)
- [Hagerty — Die-cast racing in 1/64 scale (3DBotMaker)](https://www.hagerty.com/media/video/3dbotmaker-diecast-racing-1-64-scale/)
- [Hot Wheels Racing League — Rules and Race Classes](http://www.racehotwheels.com/p/rules-and-race-classes.html)
- [Mattel Creations 커뮤니티 — Down Hill Racing Rules](https://community.creations.mattel.com/forums/topic/98688-down-hill-racing-rules/)
- [Redline Derby — 커스텀 전략(무게/휠)](https://www.redlinederby.com/topic/strategies-for-building-fat-track-cars/3867)

## 2. 설계 원칙

> **"환경 그래픽이 실제 같지 않더라도, 중력과 차량 거동만은 실제와 같아야 한다"** (요구사항)

- **실제 스케일 SI 단위 시뮬레이션**: 차 68mm/30~120g, 트랙 약 27m, 낙차 2.5m, g=9.81.
  속도·시간·점프 거리가 실제 다이캐스트 트랙과 동일한 자릿수로 나온다.
- **결정론적 고정 스텝 물리(240Hz)**: 같은 세팅 + 같은 시드 = 같은 결과. 리플레이는
  물리 프레임 기록을 그대로 재생하므로 원 주행과 완전히 일치한다.
- **1차원 트랙 구속 + 수직 자유도**: 실제 다이캐스트 트랙은 측벽이 차를 가두므로
  "스플라인을 따라가는 종방향 동역학 + 크레스트에서의 탄도 비행"이 물리적으로 올바른 모델이다.

## 3. 물리 모델 (per-step, dt = 1/240s)

트랙 스플라인을 호 길이 s로 매개화하고 각 지점의 경사 dy/ds, 수평곡률 κh,
수직곡률 κv를 전처리한다. 차량 상태는 (s, v).

```
가속   a  = −g·(dy/ds)                                  # 중력 사면 성분
수직하중 N = g·cosθ · max(0, 1 + v²·κv/(g·cosθ))         # 밸리에서 압축↑, 크레스트에서 ↓
구름저항  = Crr(휠등급)·날씨계수 · N
베어링    = F_bearing(휠등급)/m                           # 질량 무관 상수항 → 무거운 차 유리
공기저항  = ½·ρ·CdA(차체)·v² / m                          # 질량 무관 → 무거운 차 유리
횡방향 (조향 없음): 필요 구심가속 v²·κh 가 그립 μ_lat·N 을 넘으면 그 초과분만큼
            차가 바깥으로 미끄러진다 → lat 이동. 벽(또는 디바이더)에 닿으면 벽이
            부족한 구심력을 받쳐주고, 그 항력 × μ_wall × 안정성계수 만큼 종방향 감속.
            ⇒ 스크럽은 "벽에 닿아 있을 때만" 발생한다.
경로 길이:   실제로 그린 라인이 이동 거리를 정한다 — ds = v·dt·(1 − lat·κh)
            (코너 안쪽 라인은 짧고 바깥 라인은 길다)
차대차 접촉(로드): 겹치면 질량비로 밀어내고 횡 운동량을 교환한다. 지속 접촉 시
            안쪽 차의 초과 횡력이 바깥 차를 통해 벽으로 전달되어 양쪽 모두 감속한다.
롤/전복:     차를 바깥 바퀴 접지선 기준 역진자로 본다.
            넘김 모멘트 = a_lat·h_CG, 복원 = g·(t_half·cosθ − h_CG·sinθ).
            임계각 θc = atan(t_half/h_CG) 를 넘으면 전복. 순수 코너링은 μ_lat ≪ SSF 라
            절대 도달하지 않고, 벽에 비스듬히 부딪히는 트립(Δv_lat·h_CG/k² 의 각운동량)
            이나 착지 실패, 옆차와의 휠 인터록으로만 넘어간다.
주행 변동:   휠 얼라인먼트 편향(개체별 고정) + 노면 요철 노이즈가 횡방향에 더해진다.
뱅크(로드):  tan(φ) = v(s)²·κ(s)/g 로 각 지점을 기울인다(기준 차량 속도 분포 기준).
            타이어가 내야 하는 힘 = a_c·cosφ − g·sinφ, 노면 수직하중 = g·cosφ + a_c·sinφ.
            설계 속도 부근에서 타이어 부담이 0에 가까워지는 *중립* 구간이 생기고,
            여기서 미세한 속도차가 위/아래로 갈린다 = 발산 지점.
크라운(로드): h(lat) = −crown·lat². 중앙이 불안정 평형이라 작은 이탈이 증폭된다.
출발 편차:   차량 안착 위치·게이트 릴리즈의 미세한 옆 힘 (매 주행 다름).
휠 불균형:   회전각에 동기된 수직하중 변조. 위상이 매번 달라 미끄러짐 시점이 달라진다.
점프: κv < 0 이고 v²·|κv| > N 이면 접지력 상실 → 탄도 비행(중력+항력만),
      착지 시 수직 충격량에 비례한 속도 손실 + 서스펜션 스쿼시 연출
바람/노면 요철: 시드 기반 미세 노이즈 (날씨별 강도)
```

### 날씨가 물리에 주는 영향 (시각 효과만이 아님)
| 날씨 | 구름저항 | 벽마찰 μ_wall | 횡그립 μ_lat | 공기 | 노이즈 |
|---|---|---|---|---|---|
| ☀️ 맑음 | ×1.0 | 0.20 | 0.55 | ×1.0 | 미세 |
| 🌧️ 비 | ×1.6 (수막 저항) | 0.15 (미끄러운 벽) | 0.32 | ×1.05 | 중간 |
| ❄️ 눈 | ×2.6 (눈 갈아엎기) | 0.22 | 0.25 | ×1.08 | 큼 |

→ 눈 오는 날 가벼운 차 + 순정 휠은 **평지 런아웃에서 실속해 완주 실패(DNF)** 할 수 있다.
→ 비 오는 날은 코너 스크럽 손실이 작아지는 대신 전 구간 저항이 커진다. 세팅에 따라
  전혀 다른 결과가 나온다 (요구사항 충족).

### 커스터마이징 → 물리 매핑
- **출발 레인(1~4)**: 레인별 스플라인을 따로 생성 — 코너 안/바깥 레인의 실제 길이·곡률 차이가 그대로 기록 차이가 됨. UI에 레인별 실측 길이 표시.
- **차량 무게(30~120g)**: m 자체. 관성/항력비/충격 모두에 반영.
- **휠 등급(순정/FTE/폴리싱+윤활)**: Crr과 베어링 상수항.
- **무게 배분(앞/중앙/뒤)**: 코너 안정성 계수(스크럽 손실 배율).
- **차체 형상(스트림라인/스탠다드/와이드)**: CdA(공기저항)와 안정성의 트레이드오프.

## 4. 시스템 아키텍처

```
index.html            UI 패널(설정/HUD/결과/리플레이 바) + importmap
vendor/               three.module.js, OrbitControls.js (오프라인 동작)
src/
  main.js             상태머신(설정→카운트다운→레이스→결과→리플레이), 루프, 카메라
  track.js            컨트롤포인트 → Catmull-Rom 스플라인 → 레인별 호길이/경사/곡률 전처리,
                      트랙 단면 스윕 메시(4레인+측벽+디바이더), 지지대, 게이트, 중계 카메라 포인트
  physics.js          CarSim: 위 물리 모델의 240Hz 고정 스텝 적분기 (결정론적)
  carmodel.js         프로시저럴 다이캐스트 카 메시 (차체 타입/색상/휠)
  environment.js      지형(트랙 근접 클램프), 나무, 하늘/안개/조명, 비/눈 파티클, 날씨 전환
  replay.js           Recorder(물리 프레임 기록) + Player(보간 재생) + 하이라이트 자동 추출
  rng.js              mulberry32 시드 RNG
```

## 5. 리플레이 / 하이라이트 (요구사항)

- 레이스 중 모든 물리 프레임(120Hz)을 기록 → 종료 후 **타임라인 스크럽, 재생/일시정지,
  0.1×/0.25×/0.5×/1×/2× 슬로우모션** 지원.
- **하이라이트 자동 추출**: 출발, 최고 속도 순간, 점프(비행 구간, 비거리 표시), 코너 최대
  공방(스크럽 피크), 피니시 → 칩 클릭 시 해당 시점 직전으로 점프 + 자동 슬로우.
- 카메라 4종: 체이스 / **중계(트랙사이드 자동 전환, 3DBotMaker 스타일)** / 온보드 / 자유 궤도.

## 6. 단계별 로드맵

- **v1 (완료)**: 1대 타임어택. 4레인 트랙, 날씨 3종, 풀 커스텀, 리플레이/하이라이트.
- **v2 (완료)**: 랭킹/세팅 저장, 마운틴 로드 트랙 스타일, 다이캐스트 리버리,
  **고스트 카**(자기 최고 기록 재생), **2대 동시 출발**(라이벌 프리셋 4종, 실시간 갭 HUD,
  역전 하이라이트). 시뮬레이션·기록·리플레이가 모두 차량 배열 기반이라 3~4대로 확장 가능.
- **v3 (완료)**: 횡방향 동역학(원심력에 밀려 벽을 긁는 실제 거동), 로드 코스의 공용 기준선 +
  차대차 접촉, Web Audio 프로시저럴 사운드(구름/스크럽/착지/점프 정적/게이트/피니시).
- **v4 (완료)**: 롤 동역학과 사고 모델 — 트립 오버 전복, 착지 실패, 코스 이탈,
  휠 인터록, 휠 얼라인먼트 편향과 노면 요철에 의한 주행 변동.
- **v5 (완료)**: 세팅 밸런스 — 모든 선택에 반대급부를 부여. 액슬 굽힘(무게 최적점),
  휠 그립 트레이드오프, 무게중심 앞뒤 위치(출발 낙차 vs 착지 자세 vs 전복), 차폭(윤거 vs 항력).
- **v6 (완료)**: 차량 프리셋 4종(머슬/JDM 튜너/슈퍼카/랠리) — 프리셋별 실루엣과 디테일,
  마운틴 로드를 연석 + W빔 가드레일 + 갓길 + 시선유도 표지를 갖춘 실제 도로로 재구성.
- **v7 (완료)**: 결과 발산 — 뱅크(캔트) 코너, 노면 크라운, 출발 편차, 휠 불균형.
  같은 세팅 300회에서 로드 코스 기록이 4갈래로 갈리고 주행 라인 편차가 0mm → 172mm.
- v8: 4대 토너먼트 브래킷, 트랙 에디터, 코스 레코드 분리 저장.
