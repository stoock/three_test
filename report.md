# 에이전트 스킬 언어 구성이 토큰 소모량·정확도에 미치는 영향

**상태: 설계·하네스·예상 완료 / 실측 미완료 (이 환경에 API 자격증명 없음).**
아래 4장까지는 확정된 설계와 사전 예상입니다. 5장 "실측 결과"는 `run_experiment.py`를
`ANTHROPIC_API_KEY`가 있는 환경에서 실행하면 자동으로 채워집니다. **추측값을 실측값으로
제시하지 않기 위해, 측정되지 않은 수치는 비워 두었습니다.**

---

## 0. MODEL과 토크나이저 (반드시 명시)

- **MODEL = `claude-opus-4-8`** — 모든 토큰 수는 이 모델의 토크나이저에 의존합니다.
- 토크나이저: claude-opus-4-8 은 **Opus 4.7에서 도입된 토크나이저**를 사용합니다
  (Anthropic 문서 기준; 공개 오프라인 토크나이저는 제공되지 않음). 따라서 **정확한 토큰
  수는 API `count_tokens` 엔드포인트로만** 얻을 수 있고, 본 실험은 그 값을 사용합니다.
- **MODEL을 바꾸면 토큰 수·배수·비용이 모두 달라집니다** (§6 주의 참조). 다른 모델로
  재현하려면 `run_experiment.py` 상단 `MODEL` 을 바꾸고 전 과정을 재측정해야 합니다.
- 가격(가정, 실측 아님): 입력 **$5 / 1M**, 출력 **$25 / 1M** (claude-opus-4-8 리스트 가격).
  이 값은 공표 가격이며 측정치가 아닙니다 — 비용 표는 이 가정에 선형 의존합니다.

### ⚠️ 이 환경의 실측 제약 (측정된 사실)
- 컨테이너에 `ANTHROPIC_API_KEY`가 없고, `ant` OAuth 프로필도 없습니다. 자식 파이썬
  프로세스에서 사용할 자격증명이 존재하지 않아 **라이브 호출을 수행할 수 없었습니다.**
  `api.anthropic.com` 에 실제 도달은 하지만 인증에서 401이 반환됩니다(플레이스홀더 키로 확인).
- **claude-opus-4-8 은 `temperature`/`top_p`/`top_k` 파라미터를 제거**(전송 시 400)한 것으로
  **문서화**돼 있습니다 — 키가 없어 여기서 API로 직접 확인하진 못했고, 하네스가 **실행 시**
  temperature를 시도한 뒤 400이면 파라미터 없이 재시도해 실제 동작을 검증합니다(어느 경로였는지
  `temp_mode`에 기록). 문서대로라면 과제의 "temperature=0.0 / 0.7"을 이 모델에서 문자 그대로
  실행할 수 없고, 6회 반복은 **기본 샘플링의 출력 분산**만 측정하며 "temperature 차원"은 붕괴합니다.
  (온도를 실제로 받는 `claude-sonnet-4-6`/`4-5`로 MODEL을 바꾸면 온도 축을 온전히 측정할 수 있음.)

---

## 1. 실험 설계 요약

- **스킬 4종**(의미 동일, 정확한 번역), `./skills/` 아래:
  - `a_en` = `skill_en.md` (영문 전용)
  - `b_ko` = `skill_ko.md` (한글 전용)
  - `c_mix` = `skill_mix.md` (영문 골격 + 한글 병기)
  - `d_en_ko` = `skill_en_ko.md` (영문 본문 + "리뷰는 한국어로 작성" 지시만 추가)
  - 네 스킬 모두 동일 절차(의도→버그→보안→성능→가독성)와 동일 출력 형식
    (Summary/Bugs/Security/Performance/Readability, 300단어 이내, 없는 문제 금지)을 지시.
- **테스트 과제**: `./fixture/diff.txt` — 심어둔 결함과 정답은 `./fixture/ground_truth.json`에
  하드코딩. 심은 결함:
  1. `bug_cache_key_missing_tenant` — 캐시 키에서 tenant_id 누락(테넌트 간 충돌).
  2. `bug_cache_null_poisoning` — null(None) 결과를 캐싱(오염).
  3. `perf_n_plus_1` — 루프 내부 N+1 쿼리(기존 조인 → 주문별 get_user 호출).
  4. `readability_missing_tests` — 새 경로 테스트 없음. (getU→get_user 개명 자체는 정상 개선.)
  - **보안 결함 = 없음** (없는 문제를 지어내는지 확인용).

### 크기 프록시 (참고용, 토큰 수 아님 — 문자/단어 수)
| 스킬 | 문자수 | 공백기준 단어수 |
|---|---|---|
| skill_en.md | 1205 | 177 |
| skill_ko.md | 1450 | 72 |
| skill_mix.md | 2068 | 216 |
| skill_en_ko.md | 1359 | 199 |

> 주의: 문자/단어 수는 **토크나이저 토큰이 아니며**, 한국어는 공백 단어수를 과소계상합니다.
> 실제 토큰 배수는 §5.1의 count_tokens 값으로만 판단합니다.

---

## 2. 측정 절차 (`run_experiment.py`)

- **A. 입력 토큰(정확):** 변형별로
  `client.messages.count_tokens(model=MODEL, system=<스킬본문>, messages=[diff])`.
- **B. 라이브 실행:** 변형별 `messages.create` 6회 — temperature=0.0 요청 1회 +
  temperature=0.7 요청 5회. 매 응답의 `usage.input_tokens/output_tokens` 기록.
  (opus-4-8의 temperature 제약은 §0 참조.)
- **C. 정확도 채점(응답별):**
  - **재현율** — 심은 결함 4개 중 몇 개를 지적했는가. 정규식 기반 개념 재현율(실측)과,
    파일/줄 언급까지 요구하는 엄격 재현율(실측)을 함께 기록하고, **LLM-심판** 재현율도 별도 기록.
  - **허위양성** — 보안 섹션이 '문제 없음' 표기가 아닌 구체적 취약점을 주장하면 FP(실측 휴리스틱).
    지어낸 문제 개수는 **LLM-심판**이 별도 집계.
  - **형식 준수(실측)** — 5개 섹션 존재 여부, 보안 '문제 없음' 표기, 300단어 이내.
  - **출력 언어(실측)** — 한글 비율로 ko/en 판정 후 기대 언어와 비교.
- 심판 프롬프트·원응답·판정 근거는 `judge_logs.jsonl`에 전부 기록.
- 각 수치는 `results.csv`에서 '실측' 또는 'LLM-판정'으로 라벨됨.

---

## 3. 사전 예상 (실측 이전에 적음)

측정 전에 적어 두는 방향성 예상입니다. **아래는 예상일 뿐 측정값이 아닙니다.**

### 3.1 입력 토큰 (영문 대비 배수)
| 변형 | 예상 배수 | 근거 |
|---|---|---|
| a_en | 1.00× (기준) | 영문 골격만. |
| b_ko | **~1.3–1.9×** (불확실) | 한글은 토큰 밀도가 높아 문자당 토큰이 많음. |
| c_mix | **~1.9–2.6×** (가장 큼) | 영·한 양쪽을 모두 포함(골격+병기)해 최장. |
| d_en_ko | **~1.05–1.2×** | 영문 본문 + 한 줄 한국어 지시만 추가. |

### 3.2 출력 토큰·비용
- 한국어로 리뷰하는 (b)(c)(d)는 동일 내용도 **출력 토큰이 영문보다 많을 것**(~1.3–1.7×, 불확실).
- 비용은 출력 단가($25)가 입력($5)의 5배라 **출력 토큰이 총비용을 지배**.
- **핵심 가설:** 한국어 결과가 필요할 때 `(d) 영문본문+한국어지시`가 `(b) 한글전용`보다
  **입력 토큰이 적어 총비용 효율이 좋을 것**(출력은 둘 다 한국어라 비슷, 입력에서 d가 유리).

### 3.3 정확도
| 지표 | 예상 |
|---|---|
| 재현율 | 네 변형 모두 높음(~0.9–1.0). opus-4-8은 4개 결함을 대체로 잡을 것. |
| 허위양성(보안 지어내기) | 모두 ~0 예상(모델이 없는 문제를 잘 안 지어냄). |
| 형식 준수 | 모두 높음. |
| 언어 준수 | a=영어 ✓, b=한국어 ✓, d=한국어 ✓(명시 지시). **c(mix)는 불확실** — 출력 언어를
  명시하지 않고 골격이 영어라 **영어로 나올 가능성**이 있음 → 과제가 기대한 '한국어'와
  불일치할 수 있다는 것이 가장 흥미로운 예상. |

---

## 4. 예상 vs 실측 비교 (실행 후 채워짐)

`run_experiment.py` 실행 시 §5에 실측이 채워지며, 여기서 각 예상의 적중 여부를 대조합니다.
현재는 **미측정**이므로 적중/불일치를 단정하지 않습니다.

<!-- MEASURED_RESULTS -->

---

## 6. 한계 (반드시 명시)

- **표본이 매우 작음:** 스킬 1종·과제 1개·결함 4개뿐입니다. 변형별 6회 반복은 분산 감(感)을
  줄 뿐, **통계적 유의성을 주장하지 않습니다.** 어떤 결론도 이 하나의 diff에 조건부입니다.
- **토크나이저 의존성:** 모든 토큰·비용 수치는 `claude-opus-4-8` 토크나이저 기준입니다.
  **MODEL을 바꾸면 결과가 달라집니다.**
- **temperature 붕괴:** opus-4-8이 temperature를 받지 않으므로 "온도에 따른 분산" 축은
  이 모델에서 측정 불가하며, 6회는 기본 샘플링 분산만 봅니다(§0).
- **한국어 단어수:** 300단어 제한 판정은 공백 기준 단어수라 한국어에서 과소계상됩니다.
  형식 준수의 '300단어' 항목은 이 한계와 함께 해석해야 합니다.
- **클로드 코드 시스템 프롬프트/도구 오버헤드**는 4변형 공통이므로 **상대 비교(배수)에는
  영향 없음.** 단, 본 실험은 그 오버헤드를 포함하지 않는 순수 API 호출로 측정합니다.
- **라벨링:** §5의 각 수치는 '실측'(count_tokens/usage/정규식) 또는 'LLM-판정'(심판 호출)로
  구분됩니다. LLM-판정은 그 자체가 모델 출력이므로 오차가 있을 수 있습니다.

### 각주 — 프롬프트 캐싱
프롬프트 캐싱이 켜져 있으면 **청구 비용과 원시 토큰 수가 달라질 수 있습니다.** 본 하네스는
캐싱을 사용하지 않으므로 `usage.input_tokens`가 곧 청구 입력 토큰이지만, 캐싱 사용 시
`cache_read_input_tokens`(≈0.1×)와 `cache_creation_input_tokens`(≈1.25×)로 분리되어
동일 원시 토큰이라도 실제 청구액이 달라집니다.

---

## 부록. 재현 방법

```bash
pip install anthropic
export ANTHROPIC_API_KEY=...        # 이 환경에는 없음
python run_experiment.py --self-test   # API 없이 채점 로직 검증(통과 확인됨)
python run_experiment.py --dry-run     # API 없이 전체 파이프라인 E2E 검증(스텁, 통과 확인됨)
python run_experiment.py               # 실측: results.csv, judge_logs.jsonl 생성, report.md 채움
```

### API 키 없이 이미 검증한 것 (offline)
- **채점 로직**(`--self-test`): 한/영 응답·허위양성·300단어 초과 케이스 판정 정확 — 통과.
- **전체 파이프라인**(`--dry-run`, 스텁 클라이언트): count_tokens 호출 → 라이브 호출 →
  temperature 400 폴백 경로 → LLM-심판 JSON 파싱·로깅 → results.csv(21열, 실측/LLM-판정
  라벨) → report.md 표 채우기까지 24행 E2E 생성 — 통과. (숫자는 스텁이라 무의미; 배관만 검증.)
- **스킬 4종 등가성**: 네 파일 모두 5개 섹션명 + 공통 불변식(300단어·지어내기 금지·파일/줄
  인용) 보유 → 정확한 번역 관계 확인.
- **fixture 정합성**: 심은 결함 4종 모두 존재, 실제 보안 취약점 없음(정답 `none`과 일치).
- **확인된 제약(측정된 사실)**: 이 컨테이너에 인증 수단 없음(SDK가 auth method 미해결),
  opus-4-8용 오프라인 토크나이저 부재(SDK·tiktoken·HF 모두 없음) → **실측만 API 키 대기.**
